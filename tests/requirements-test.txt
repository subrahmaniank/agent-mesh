# Test dependencies. The suite needs no Docker: Cedar policies run against the
# real engine in-process, and both adapters run against mocked backends.
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
pyyaml>=6.0

# Evaluates cedar/policies.cedar directly, so the authorization logic stays
# verifiable without cedar-agent running.
cedarpy>=4.12.0

# Temporal's time-skipping test server, for the saga and HITL workflow tests.
temporalio>=1.5.1
