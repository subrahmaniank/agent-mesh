[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    integration: requires a live gateway/Temporal service (deselect with '-m "not integration"')
filterwarnings =
    ignore::DeprecationWarning:pkg_resources.*
